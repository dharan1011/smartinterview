#include <iostream>
#include <vector>
#define fast cin.sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
using namespace std;

void solve();

int main(){
    fast
    int T;
    cin >> T;
    while(T--){
        solve();
    }
    return 0;
}